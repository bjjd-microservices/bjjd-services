apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-mgmt-service
  labels:
    app: user-mgmt-service
  #namespace: default
spec:
  replicas: 1
  minReadySeconds: 45
  selector:
    matchLabels:
      app: user-mgmt-service
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: user-mgmt-service
    spec:
      containers:
      - name: user-mgmt-service
        image: rajivbansal2981/user-mgmt-service:0.0.1-RELEASE
        imagePullPolicy: IfNotPresent #Always
        ports:
          - name: http
            containerPort: 5379 # The port that the container exposes
          - name: multicast
            containerPort: 5701 # The port that Hazelcast exposes
        env:
        - name: DB_HOSTNAME
          valueFrom:
             configMapKeyRef:
              name: user-mgmt-service-configmap
              key: database.hostname #MySQL db host name
        - name: DB_NAME
          valueFrom: 
             configMapKeyRef:
                 name: user-mgmt-service-configmap
                 key: database.name
        - name: DB_USERNAME
          valueFrom: 
             configMapKeyRef:
                 name: user-mgmt-service-configmap
                 key: database.username
        - name: DB_PASSWORD
          valueFrom:
             secretKeyRef:
                name: user-mgmt-service-secrets
                key: database.password
        - name: DB_PORT
          valueFrom: 
             configMapKeyRef:
                 name: user-mgmt-service-configmap
                 key: database.port
        - name: SPRING_ACTIVE_PROFILE
          valueFrom: 
             configMapKeyRef:
                 name: user-mgmt-service-configmap
                 key: spring.active.profile
      restartPolicy: Always
      terminationGracePeriodSeconds: 60
---
apiVersion: v1
kind: Service
metadata:
  name: user-mgmt-service
  labels: #PODS
    app: user-mgmt-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: user-mgmt-service
  ports:
   - name: hazelcast
     port: 5701
   - name: app
     port: 5379