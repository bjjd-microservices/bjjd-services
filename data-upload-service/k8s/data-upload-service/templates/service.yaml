apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-upload-service.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "data-upload-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-upload-service.selectorLabels" . | nindent 4 }}